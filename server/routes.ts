/**
 * Point d'entrée des routes de données AxeCompta.
 * L'implémentation est éclatée dans ./routes/* pour rester maintenable ;
 * ce fichier n'existe que pour conserver le point d'import historique (./server/routes).
 */
export { default } from './routes/index';

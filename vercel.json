{
  "buildCommand": "prisma generate && vite build && esbuild server/vercelHandler.ts --bundle --platform=node --format=esm --packages=external --outfile=api/index.js",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api" }
  ]
}
